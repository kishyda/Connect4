package Logic;
public class Main
{
	public static void main(String[] args) {
	//	System.out.println("Connect 4");
	//	boolean p2p = false;
	//	int cpuLevel = 1;
	//	Game game = new Game(p2p, cpuLevel); //p2p flag=false, 1=hard level 0=easy level
	//
	//	//game.runGameLoop(); //uncomment to start automatic terminal loop
	//
	//	//example use for step method: loop while game is not over, input a move 
	//    while(!game.getGameOver()) {
	//        if(p2p || activePlayer==0) //HUMAN PLAYER STEP
	//        {
	//	        Move move = new Move(YOUR_GET_ROW, YOUR_GET_COL); //YOUR MOVE ROW/COL GETTER METHOD GOES HERE
	//               while(!game.checkLegalMove(game.board, move, game.activePlayer)) {
	//	            Move move = new Move(YOUR_GET_ROW, YOUR_GET_COL); //YOUR MOVE ROW/COL GETTER METHOD GOES HERE 
	//	                                                             //(PREVIOUSLY SELECTED MOVE WAS NOT LEGAL)
	//	        }
	//	        game.step(move, game.activePlayer); 
	//	        game.activePlayer = (game.activePlayer+1) % 2;
	//	     }
	//	     else if(!p2p && game.activePlayer==1) { //COMPUTER PLAYER STEP, it will decide move automatically
	//	         game.step(game.activePlayer);
	//	         game.activePlayer = (game.activePlayer+1) % 2;
	//	     }
	//}
	//Public utility methods
	//game.board.getBoard(); //get board (public)
	//game.board.getWinner(); //get winner (public)
	
    }
}
